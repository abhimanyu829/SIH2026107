# Phase 4 - Supabase PostgreSQL + Qdrant Cloud + BGE-M3 embedding.
# BGE-M3 loads via transformers (CLS pooling); sentence-transformers 6.x
# is incompatible with this repo's modules.json, so it is NOT a dependency.
psycopg2-binary>=2.9
qdrant-client>=1.12
transformers>=4.44
torch>=2.2
numpy>=1.26
