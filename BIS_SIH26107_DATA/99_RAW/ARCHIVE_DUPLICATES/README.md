# ARCHIVE_DUPLICATES

Byte-identical **copies** of the 4 source files Phase 2 found fully redundant (F012, F023, F027, F028). The originals remain untouched in the upload folder and every one of their rows is still accounted for in `92_VALIDATION/DATA_LOSS_AUDIT.csv` with disposition `DUPLICATE_ARCHIVED`.

Nothing here was deleted, moved or modified. sha256 digests are re-verified against Phase 1's inventory in `92_VALIDATION/ARCHIVE_VALIDATION.csv`.
